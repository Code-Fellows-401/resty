import { setupWorker, rest } from 'msw';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

describe('some test here', () => {
	it('sdome test here', () => {
		render(<somecomponentHere />);
	});
});
